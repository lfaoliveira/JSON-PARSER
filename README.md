# A text-based game engine where I do definition-oriented programming to parse a complex JSON file containing the structure/story of a Dungeon Crawling Game.

## Run instructions:
## Conda:
`conda env create -f environment.yaml`
`conda activate hipermidia`
------------
## Pip:
`pip install -r requirements.txt`
-----------------
